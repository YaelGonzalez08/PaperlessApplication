<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Perímetro superior en línea punteada -->
    <item android:top="0dp">
        <shape android:shape="line">
            <stroke
                android:width="1dp"
                android:color="@color/primaryColor"
                android:dashWidth="1dp"
                android:dashGap="1dp"/>
        </shape>
    </item>
</layer-list>